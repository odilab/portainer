import { EditorView } from "@codemirror/view";
import { type JSONValidationOptions } from "../features/validation";
/**
 * Instantiates a JSONValidation instance with the YAML mode
 * @group Codemirror Extensions
 */
export declare function yamlSchemaLinter(options?: JSONValidationOptions): (view: EditorView) => import("@codemirror/lint").Diagnostic[];
